# tests/Application.Tests

Unit tests for the managed Application layer (frame loop, registry, state machine, settings model). Placeholder — test framework choice deferred until milestone 3; Application is Unity-free by design so plain xUnit/NUnit works.
