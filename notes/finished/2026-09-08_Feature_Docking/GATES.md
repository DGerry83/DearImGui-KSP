# Frozen Acceptance Gates: Window Docking (ISSUES #011)
## Frozen At: 2026-09-08T12:21:33-0400
## Source: [ARCHITECTURE_CONTRACT.md](ARCHITECTURE_CONTRACT.md)

| Gate ID | Criterion | Owner | Verdict | Evidence |
|---------|-----------|-------|---------|----------|
| G1 | Native debug and release builds pass with pin enforcement green (`build.bat`, `build_release.bat`), native harness builds, and `DearImGuiKSPNative_GetVersion()` returns 9 | Scope A implementer | PASS | `build.bat` exit 0 (pin check green, DLL deployed); `build_release.bat` exit 0; `build_harness.bat` exit 0; `build/harness.exe` exit 0 "HARNESS PASS"; `DearImGuiKSPNative_GetVersion()` returns 9 at `DearImGuiKSPNative/src/DearImGuiKSPNative.cpp:42` |
| G2 | `dotnet build DearImGui-KSP.slnx` succeeds and the full xUnit suite passes including new docking tests (settings default/persistence, enum ordinal pins, facade guards) | Scope B implementer | PASS | `dotnet build` 0 err/0 warn; `dotnet test` 191/191 (baseline 178, +13: DockingEnumPinTests, DockingFacadeGuardTests, DockingModeApplierTests, settings default/round-trip) |
| G3 | Handshake lockstep: managed `ExpectedNativeVersion` equals native GetVersion (both 9); the version-mismatch failure path remains intact | Scope B implementer | PASS (managed half) | `NativeBridge.cs:44` ExpectedNativeVersion = 9 with history comment "9: ISSUES #011 docking"; handshake block otherwise untouched (Initialize mismatch path → InitErrVersionMismatch → FailureKind.VersionMismatch); existing mismatch mapping test green in 191/191 suite. Native GetVersion=9 is Scope A's gate (G1) |
| G4 | `docking` key: defaults to true when absent from settings.cfg; setting it false disables docking live (ConfigFlags cleared); toggle round-trips through the control panel and persists | Scope B implementer | PASS | Unit portion per Scope B report (default/absent-key/persist/round-trip tests). In-game half: user-verified 2026-09-08 — toggle starts ON, OFF undocks everything live, ON reapplies the demo layout; settings.cfg persists the value |
| G5 | In-game D3D11 (pinned GL-capable instance): demo windows dock per the built layout; windows can be dragged to dock/undock; uGUI click-blocker and IMGUI capture grab still engage; clampWindowsToViewport does not fight docked windows; no visual/theming regression in non-docked use. User-verified | User | PASS | User runs 2026-09-08: initial run found the demo layout never appeared (4px auto-resize clamp defect, root-caused to imgui.cpp:20831-20836, fixed via explicit dockspace height + aux windows default visible); re-run PASS — layout docks (plots over benchmark), drag dock/undock, green KSP drop preview, input shields engage, clamp coexists with docked windows, no theming regression, no auto-resize loop. Instance KSP.log: 28 `[DearImGuiKSP]` lines, zero ERR/EXC/WRN |
| G6 | In-game OpenGL Core (`-force-glcore`): same docking smoke passes. User-verified | User | PASS | User run 2026-09-08 (`-force-glcore`): same results as the D3D11 smoke (dock/undock, toggle off/on, green preview); post-fix demo layout is backend-agnostic C#; final confirmation included in the user's post-fix re-check |
| G7 | D16-compat regression (ReformTestInstance: Deferred, TUFX, Scatterer, Parallax, Cinematic Shaders/Recorder, IMGUI mods): docking on, no breakage; IMGUI mods unaffected. User-verified | User | PASS | User run 2026-09-08 (ReformTestInstance): full pass — all compat mods behaved, IMGUI mods unaffected, docking works. Orchestrator log sweep found one recoverable ImGui error ("Must call EndChild() and not End()!", 13:47:09): root cause = the dockspace-host main demo window could itself be docked (unsupported upstream, imgui.cpp:8908-8909 path; official demos declare hosts NoDocking); fixed demo-side via `noDocking: true` + docs guidance. User re-verification run 2026-09-08 (GL instance, defect is instance-agnostic demo code): main window refuses to dock, aux docking unaffected, log clean (25 `[DearImGuiKSP]` lines, zero ERR/EXC/WRN) |
| G8 | Docs updated (10-api-fundamentals settings row, 20-widgets docking section, 70-troubleshooting limitations incl. no viewport-external windows and no layout persistence); PIN_RECORD/README `-docking` wording fixed; ISSUES #011 closed and TRACKER.md synced | Scope C implementer | PASS (docs portion only; ISSUES #011 closure + TRACKER.md sync stay open for the parent after user in-game verification G5-G7) | `docs/10-api-fundamentals.md` `docking` row (default true) + sample settings.cfg updated; `docs/20-widgets.md` "Window docking" section (settings key + panel toggle, DockSpace/DockSpaceOverViewport semantics, curated DockBuilder recipe: split the live node, dock by exact title, Finish — noDocking overloads documented); `docs/70-troubleshooting.md` four new limitations (no viewport-external docked windows, no layout persistence, clampWindowsToViewport skips docked windows, live-toggle undocks). PIN_RECORD/README wording = Scope A (G1). File diffs on `feature-Docking` |

## Session Verdict
- **Verdict**: CONTINUE
- **Reason**: All gates G1-G8 PASS (G8's ISSUES-closure portion completed by the parent at session close). Audit (AUDIT_REPORT.md, 2026-09-08): CONTINUE, no gate-blocking violations; minor docs findings V1-V3 remediated pre-merge.
