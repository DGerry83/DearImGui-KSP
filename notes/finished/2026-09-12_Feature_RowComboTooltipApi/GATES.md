# Frozen Acceptance Gates: Consumer API Additions — Row / Combo / Tooltip (1.3.0)
## Frozen At: 2026-09-12T20:49:06Z
## Source: [ARCHITECTURE_CONTRACT.md](ARCHITECTURE_CONTRACT.md)

GATES.md is frozen. Post-freeze modification = automatic audit failure
(reference\07-frozen-gates.md; BugfixPlanning stricter KILL rule applies).

| Gate ID | Criterion | Owner | Verdict | Evidence |
|---------|-----------|-------|---------|----------|
| G1 | `dotnet build DearImGui-KSP.slnx -c Release` succeeds with all changes, and no existing public API signature is modified or removed (diff of public surface shows additions only) | Scope A implementer + Auditor | PASS (Scope A part; auditor re-check pending) | `dotnet build DearImGui-KSP.slnx -c Release` succeeded 0 warnings/0 errors; `git diff --numstat` on the 12 modified files shows deletions = 0 everywhere (insertions-only); new surface is 4 new library files (`RowState.cs`, `DearImGuiKSP.Row.cs`, `DearImGuiKSP.Combo.cs`, `DearImGuiKSP.Tooltip.cs`) + additions in `ImGuiEx.cs` (Row/RowScope), `ImGuiNative.cs`/`ImGuiInternal.cs` (internal) |
| G2 | Row API per contract: `ImGuiEx.Row()`/`Row(float)` + `RowScope` exist; inside a row the first item is never preceded by SameLine and every later item is; nested `Row()` is an inert no-op in release; row state resets at frame open and on fault unwind; unit tests for row state pass; in-game: the demo 4×4 button grid renders as 4 rows inside an `autoResize: true` window and inside a `ScrollRegion` | Scope A (unit) + User (in-game) | PASS | `ImGuiEx.Row()`/`Row(float)`/`RowScope` in `Application/Api/ImGuiEx.cs`; `RowState` in `Application/RowState.cs` (TryPush nested rejection, OnRowItem first-item rule); reset wiring at `FrameLoopOrchestrator.cs:132` + `UnwindOpenScopes`; 20 widget hook sites (+GradientButton post-audit addendum); 18 `RowStateTests` pass. IN-GAME (user, 2026-09-12, D3D11): layout showcase pass — selector row, label+slider row, 4×4 tinted grid renders as 4 rows in the autoResize main window; rows inside the ScrollRegion pass; state-tint click cycle works |
| G3 | Combo API per contract: `Combo(string, ref int, IReadOnlyList<string>)` exists; null/empty items render a disabled `"(none)"` combo whose popup never opens and never throws; out-of-range index shows `"(none)"` without exception and without rewriting the ref; a real selection updates the ref, returns true, highlights the item, and closes the popup; unit tests for preview resolution pass; in-game: the 50+ item demo combo opens and scrolls without clipping | Scope A (unit) + User (in-game) | PASS | `Combo` + `ResolveComboPreview` + `ApplyComboSelection` in `Application/Api/DearImGuiKSP.Combo.cs`; disabled-empty path via `igPushItemFlag(ImGuiItemFlags_Disabled)`; try/finally with conditional `EndCombo` + `PopItemFlag`; 12 `ComboLogicTests` pass. IN-GAME (user, 2026-09-12, D3D11): 64-item combo opens, scrolls without clipping, selection updates preview + echo; out-of-range "Recovered combo" shows "(none)", opens, selection repairs index (behavior verified item-by-item against spec — user confirmed exact match). Empty-list disabled path: demo widget added post-verification ("Empty combo (disabled)"), quick look pending user's next launch |
| G4 | Tooltip API per contract: `Tooltip(string)` exists; shows on hover of the previous item with stock `ImGuiHoveredFlags_ForTooltip` delay semantics; null/empty text and no-preceding-item are no-ops; multi-line text renders and long text wraps at font-size-proportional width; **no variadic P/Invoke is introduced** (`ImGuiNative.cs` convention intact — tooltip composed from non-variadic exports); hovered-flags pin test + guard tests pass; in-game: tooltips visible in demo on a button, a slider, and a grid cell | Scope A (unit) + User (in-game) | PASS | `Tooltip` in `Application/Api/DearImGuiKSP.Tooltip.cs` (compose path, nested try/finally); wrap verified non-double vs pinned imgui.cpp BeginTooltipEx; `igSetItemTooltip` NOT bound; 8 `TooltipGuardTests` pass (ForTooltip=1<<12 / Disabled=1<<6 pins, wrap pin 35, no-op guards). IN-GAME (user, 2026-09-12, D3D11): tooltips pass — visible on the selector button, the pair-row slider, and grid cell 01 (multi-line), with stock hover delay and wrapping |
| G5 | Full xUnit suite passes: `dotnet test DearImGui-KSP.slnx` — all pre-existing tests plus new RowState/Combo/Tooltip/enum-pin tests green | Scope A implementer + Auditor | PASS | `dotnet test DearImGui-KSP.slnx`: Passed! Failed: 0, Passed: 229, Skipped: 0, Total: 229 (191 pre-existing + 38 new: 18 RowState + 12 Combo + 8 Tooltip) |
| G6 | Docs updated: `docs/20-widgets.md` gains Layout (Row), Combo, and Tooltip sections and the "No public SameLine"/"no combo" absence notes (:644-651) are gone; `docs/60-migration-from-imgui.md` gains mapping rows for BeginHorizontal→Row, IMGUI dropdown→Combo, GUI.tooltip→Tooltip with stale "stack vertically" advice removed; `docs/70-troubleshooting.md:31` says handshake version 9; `docs/00-getting-started.md:90` says 1.3.0 | Scope B + Auditor (greps) | PASS (Scope B part; auditor re-check pending) | All four docs files updated per criterion (plus the second "version 7" occurrence at 70-troubleshooting.md:26 — see D-B1): 20-widgets gains `## Layout: Rows (ImGuiEx.Row)` (incl. 4×4 `##cell_{x}_{y}` grid example + identical-labels-## note), `## Combo`, `## Tooltip`; end-of-page SameLine bullet removed, absence bullet no longer lists combo. 60-migration mapping rows added (BeginHorizontal→Row, BeginVertical→none, hand-rolled dropdown→Combo, GUI.tooltip→Tooltip); "vertical-first, with a known gap" section rewritten; checklist item 5 amended. 70-troubleshooting says version 9 (:26 prose + :31 log quote, verified vs `NativeBridge.cs:44`); known-limitation bullet amended to "Horizontal layout is row-scoped". 00-getting-started says **1.3.0** with `(1, 3)` attribute example. Verification greps over docs/ (case-insensitive): `No public SameLine\|no public horizontal\|vertical-first\|post-release candidate\|stack vertically for now\|version 7\|1\.0\.0\|no public checkbox, combo\|no combo\|no tooltip` → zero matches; remaining `SameLine` hits are only the accurate "no public raw SameLine" residual limitation (designed API fact). Detail in PROGRESS_LOG.md "Scope B — Docs". |
| G7 | Demo compiles and showcases all three APIs: row examples, a 4×4 per-state-tinted button grid with `##` cell ids, a combo with 50+ items, and tooltips; demo csproj `<Version>` and `DearImGuiKSPDemo.version` bumped in lockstep (1.3.0) | Scope C + Auditor | PASS (Scope C part; auditor re-check pending) | `dotnet build DearImGui-KSP.slnx -c Release` succeeded 0 warnings/0 errors; showcase in `DemoConsumer.cs` `DrawLayoutShowcase()` (:438, called at :328 inside the autoResize main window): selector row `ImGuiEx.Row()` (:447), label+control row (:463), 4×4 grid via `Row(GridRowSpacing)` in loops with `##camslotNN` ids + stacked `ImGuiEx.StyleColor` per-state tints (:472-486, :532-564), 64-item `Combo` with text readout (:489), out-of-range-seeded `Combo` + re-orphan button (:496-504), tooltips on a button/slider/grid cell with multi-line text (:458, :468, :595), rows inside `ImGuiEx.ScrollRegion` (:509-525); version pair 1.3.0 lockstep (csproj :10, template :5, built assembly 1.3.0.0); `KSPAssemblyDependencyEqualMajor` untouched; in-game check pending user (G9) |
| G8 | Release prep complete: library csproj `<Version>` and `DearImGuiKSP.version` both 1.3.0; CHANGELOG has "1.3.0" and "Demo 1.3.0" sections; `package_release.bat` runs and produces `dist\DearImGuiKSP-1.3.0.zip` and `dist\DearImGuiKSPDemo-1.3.0.zip` plus symbols archive; native handshake unchanged (v9) and `DearImGuiKSPNative/` sources untouched | Parent + Auditor | PASS (parent part; auditor re-check pending) | Packaging log 2026-09-12: "Packaging library version 1.3.0, demo version 1.3.0" → both zips + `dist\symbols\DearImGuiKSPNative-1.3.0.pdb`; library zip entry listing verified (DLLs, docs, CHANGELOG; no settings.cfg/PDB); csproj/.version pairs read 1.3.0; `git status --porcelain -- DearImGuiKSPNative/src include vendor *.bat` empty (sources untouched); handshake v9 per `NativeBridge.cs:44` |
| G9 | In-game acceptance (user-driven, pinned test instance): all three APIs exercised via the demo on D3D11 with no rendering regressions; GL smoke pass on the GL-capable instance; existing demo widgets (knob, wheel, plots, docking) behave as before | User | PASS | User report 2026-09-12: layout showcase pass; ScrollRegion rows pass; combos pass (verified item-by-item against spec); tooltips pass; no regression on existing demo widgets (knob/wheel/plots/docking) — pass; GL smoke (`-force-glcore`, pinned GL-capable instance) — pass. Post-verification demo addition ("Empty combo (disabled)" + recovered-index echo) pending a quick look on next launch |
| G10 | Native Interop & Hot-Path Checklist verdicts recorded for every new binding chunk in PROGRESS_LOG.md (process-global state / hot-path allocation / acquisition symmetry — "N/A + reason" acceptable) | Scope A implementer + Auditor | PASS (Scope A part) | PROGRESS_LOG.md "Native Interop & Hot-Path Checklist chunk verdicts (G10)": per-chunk verdicts recorded for M1 Row (SameLine overload + hook), M2 Combo (BeginCombo/Selectable/PushItemFlag), M3 Tooltip (BeginTooltip/wrap-pos/GetFontSize + IsItemHovered flags overload) — process-global state N/A+reason for all three; allocation and symmetry verdicts as recorded |

## Session Verdict
- **Verdict**: CONTINUE
- **Reason**: All ten gates PASS (G1–G8, G10 verified with evidence; G9 + in-game halves of G2/G3/G4 user-verified 2026-09-12 on D3D11 + GL smoke). No FAILs. One demo-only follow-up noted in G3/G9 (empty-combo widget quick look) — demo clarity addition, not a library change.
