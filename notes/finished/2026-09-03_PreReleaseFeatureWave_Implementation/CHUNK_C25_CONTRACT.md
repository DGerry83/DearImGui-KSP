# Chunk Contract: Release Packaging + D33 OpenGL Decision Record
## Plan: DearImGui-KSP Pre-Release Feature Wave
## Date: 2026-09-05
## Chunk ID: C25
## Advances Milestone: M8 — Release packaging + OpenGL decision point (the only M8 chunk; packaging is atomic)

### Scope

The wave is code-complete and all gates through C36 have passed in-game
(incl. the verboseLogging KSP.log inspection — clean). This chunk produces
the release artifacts and records the OpenGL scheduling decision. Plan M8
row: "Release zip (Fonts/ + docs/), lockstep version bump, D33 workload
assessment and decision record."

1. **Lockstep version bump: 0.1.0.0 → 0.2.0.0** (minor bump for a feature
   wave; still 0.x, so API may break on minors per D17). Touch exactly:
   - `DearImGuiKSP/DearImGuiKSP.csproj` `<Version>0.1.0.0</Version>` → `0.2.0.0`
   - `DearImGuiKSPDemo/DearImGuiKSPDemo.csproj` same
   The AVC `.version` files and `Readme.txt` under `GameData/` are
   GENERATED by KSPBuildTools from the csproj + the project-root `.version`
   templates (which carry no VERSION field) + repo `README.md`
   (csproj line ~40) — do not hand-edit generated files; rebuild and verify
   the regenerated GameData copies say 0.2.0.0. There is no native version
   resource (plain cl.exe) and no new export this chunk, so the handshake
   stays **v6**.
2. **Vendor upstream cimplot's MIT LICENSE** into
   `DearImGuiKSPNative/vendor/cimplot/` (C24 accepted follow-up). The
   generator pin is in `vendor/PIN_RECORD.md`; fetch the LICENSE file from
   the cimplot repo at that commit (curl the raw file from GitHub — the
   `%TEMP%\c11_scratch` clone may be gone). Byte-identical to upstream, no
   edits. Add the pin row note in PIN_RECORD.md if it references the missing
   LICENSE.
3. **Packaging script `package_release.bat`** at repo root (same plain-cmd
   idiom as `DearImGuiKSPNative/build_release.bat`; may shell out to
   PowerShell `Compress-Archive` for zipping). It must, in order:
   - run `DearImGuiKSPNative\build_release.bat` (native →
     `GameData/DearImGuiKSP/PluginData/`) and `dotnet build
     DearImGui-KSP.slnx -c Release` (managed → GameData staging);
   - read the version from `DearImGuiKSP/DearImGuiKSP.csproj` (do NOT
     hardcode 0.2.0.0 in the script);
   - assemble `dist\DearImGuiKSP-<version>.zip` containing
     `GameData/DearImGuiKSP/` RECURSIVE — `Plugins/DearImGuiKSP.dll`,
     `PluginData/DearImGuiKSPNative.dll`, `Fonts/` (2 TTF + OFL.txt),
     `Textures/toolbar.png`, `settings.cfg`, `DearImGuiKSP.version`,
     `Readme.txt` — PLUS the modder docs copied to
     `GameData/DearImGuiKSP/Docs/*.md` (all 8 files from `docs/`), PLUS
     `CHANGELOG.md` copied to `GameData/DearImGuiKSP/Docs/CHANGELOG.md`;
   - assemble `dist\DearImGuiKSPDemo-<version>.zip` containing
     `GameData/DearImGuiKSPDemo/` recursive (DLL + .version) — demo stays a
     separate install (D7);
   - both zips root at `GameData\...` (extract-into-KSP-root layout);
   - **exclude every `.pdb`** from both zips;
   - fail loudly (nonzero exit) if any expected input file is missing.
4. **`CHANGELOG.md` at repo root** (spec §10.5 mandates a per-release
   changelog listing API additions/deprecations/removals). Entry
   `0.2.0.0 — first public release`: one-paragraph summary of the library,
   the public API surface (windows/scopes, widgets incl. toggle/knobs/
   wheels/spinners, gradients, plotting incl. `SetupAxesAutoFit`, tweens,
   theming, control panel), the 0.x breakage policy, and the install
   requirement (KSP 1.12.x, Windows x64, D3D11). Plain-language, modder
   audience (docs tone).
5. **D33 follow-up decision, recorded as D35** appended to
   `notes/finished/2026-07-29_DesignSpec_DearImGuiKSP_UI_Library/DECISION_LOG.md`
   (existing table format: `| # | Date | Decision | Alternatives Considered
   | Rationale |`; D34 is the last row, date 2026-09-05). Text:
   - Decision: **OpenGL ships after release.** 0.2.0.0 releases D3D11-only;
     OpenGL (imgui_impl_opengl3 + embedded loader + 1.92.9 texture protocol,
     design already documented per D20) is scheduled post-release, gated on
     a clean GL test environment. Docking (#011) stays behind OpenGL.
   - Alternatives: GL in this release; GL before any release.
   - Rationale (the honest workload assessment D33 demands): the GL code
     work is small and backend-isolated, but validation dominates —
     CinematicShaders and CinematicRecorder fail under GL (D20), so the D16
     instance cannot produce actionable GL results; shipping GL unvalidated
     would violate the failure-handling spec. The wave added zero GL cost
     (all widgets are draw-list-level, backend-agnostic).
6. **`.gitignore`**: add `/dist/` (zips are release artifacts, never
   committed).
7. **`README.md` status paragraph refresh**: it still says "Status:
   feature-complete … (2026-09-03). All milestones M1–M6 …" — update to
   reflect the completed wave (M1–M7 + polish), version 0.2.0.0, first
   public release. This file becomes the shipped `Readme.txt`, so keep the
   player/modder-facing tone; do not remove the build/install/layout/credits
   sections.

### Inputs
- `DearImGuiKSP/DearImGuiKSP.csproj`, `DearImGuiKSPDemo/DearImGuiKSPDemo.csproj`
  (`<Version>` + the KSPBuildTools `CopyToGame`/`Readme.txt` targets).
- `DearImGuiKSPNative/build_release.bat` (native release build idiom),
  `vendor/PIN_RECORD.md` (cimplot generator pin).
- `GameData/` staging tree (current, post-C36), `docs/` (8 files),
  `.gitignore`, `README.md`.
- `notes/finished/2026-07-29_DesignSpec_DearImGuiKSP_UI_Library/DECISION_LOG.md`
  (D17, D20, D33 rows; table format), spec §10.5 (update path).
- M8 row: `notes/active/2026-09-03_NewProject_PreReleaseFeatureWave/IMPLEMENTATION_PLAN.md`
  line ~440.

### Outputs
- Both csproj version bumps; regenerated GameData `.version`/`Readme.txt`
  (via build, gitignored — verify content only).
- `vendor/cimplot/LICENSE` (+ PIN_RECORD note if applicable).
- `package_release.bat`, `CHANGELOG.md`, `.gitignore` (`/dist/`),
  `README.md` (status refresh), `DECISION_LOG.md` (D35 row).
- `dist/DearImGuiKSP-0.2.0.0.zip` + `dist/DearImGuiKSPDemo-0.2.0.0.zip`
  (gitignored artifacts).

### Constraints
- No shipping-code changes (the version bump is the only code-tree edit).
- Vendored LICENSE byte-identical to upstream at the pinned commit.
- Zip denylist: no `.pdb`, no source, no `notes/`, no `tests/`, nothing
  outside the two GameData trees + the Docs additions.
- `dotnet build` 0/0 and `dotnet test` 99/99 must stay green; native
  release build 0 errors (1 tolerated C4190).
- IMPEDIMENTS.md entry for any deviation; do not invent new scope (no CKAN
  metadata — spec §12 defers it; no GitHub release creation — the user
  publishes).

### Verification
- Run `package_release.bat` end-to-end from a clean `dist/`; both zips
  exist with the version in their names.
- List both zips (`unzip -l` or PowerShell) and diff against the expected
  manifest: every required file present, zero `.pdb`, Docs has all 8 .md +
  CHANGELOG.md, `.version` files inside the zips say 0.2.0.0.
- FileVersion of both managed DLLs in the zip = 0.2.0.0
  (`[System.Diagnostics.FileVersionInfo]::GetVersionInfo`).
- `dotnet build DearImGui-KSP.slnx` 0/0; `dotnet test` 99/99; native
  release build 0 errors.
- In-game (USER gate, report readiness): install BOTH zips into a clean KSP
  (no prior DearImGuiKSP) in the full D16 environment → game loads, toolbar
  button + settings panel work, demo windows render, KSP.log clean. That
  plus the recorded D35 completes M8.

### Rollback
- Version bump is two lines; script/changelog/docs are additive. `git
  checkout -- .` for tracked files; delete `dist/`.
