# Application/Theming

Created in milestone M3 of the pre-release feature wave
(`notes/active/2026-09-03_NewProject_PreReleaseFeatureWave/IMPLEMENTATION_PLAN.md`).

Planned contents:

- `ThemeEngine.cs` — applies a named theme preset to the global ImGui style (D34).
- `ThemePresets.cs` — the "ksp" and "dark" value tables (colors, rounding, padding, gradients).
- `KspPalette.cs` — tunable `Color32` constants from design spec §6.1.
