# Troubleshooting

> Authored in milestone M7 of the pre-release feature wave (spec §8.2, D31).
> Planned contents: common failure modes, log locations, and the 16-bit `ImDrawIdx` limit — trigger (draw lists above 65,535 vertices) and remedy (enable `RendererHasVtxOffset` or 32-bit indices, D32).
